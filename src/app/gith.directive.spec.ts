import { GithDirective } from './gith.directive';

describe('GithDirective', () => {
  it('should create an instance', () => {
    const directive = new GithDirective();
    expect(directive).toBeTruthy();
  });
});
